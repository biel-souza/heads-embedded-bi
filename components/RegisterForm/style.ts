import { Form } from "formik";
import styled from "styled-components";

export const RForm = styled(Form)`
  width: 100%;
  flex-wrap: wrap;
  margin-top: 10px;
  padding: 30px;
  display: flex;
  border-radius: 10px;
`;
